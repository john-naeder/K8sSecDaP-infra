---
# =============================================================================
# Kubernetes Cluster Inventory
# AUTO-GENERATED by sync-inventory.sh — 2026-04-30 00:05:56
#
# Sử dụng Tailscale IP để SSH vào các node.
# KHÔNG sửa file này trực tiếp — sửa nodes.env rồi chạy lại sync-inventory.sh
# =============================================================================

all:
  children:
    masters:
      hosts:
        userver-master:
          ansible_host: 100.120.16.113
    workers:
      hosts:
        userver-worker-home:
          ansible_host: 100.119.246.28
        userver-worker-1:
          ansible_host: 100.95.126.102

    # Group gộp tất cả K8s nodes
    k8s_cluster:
      children:
        masters:
        workers:

